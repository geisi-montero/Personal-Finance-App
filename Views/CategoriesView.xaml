<UserControl x:Class="FinanzasApp.Views.CategoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:FinanzasApp.Models"
             Background="{StaticResource ContentBgBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="20,14" Margin="0,0,0,16">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.04" BlurRadius="10" ShadowDepth="1"/>
            </Border.Effect>
            <DockPanel>
                <Button DockPanel.Dock="Right" Style="{StaticResource PrimaryButton}"
                        Command="{Binding AddCommand}" Content="+ Nueva Categoria" Padding="14,8"/>
                <TextBlock DockPanel.Dock="Left" Text="Gestiona tus categorias" FontSize="13"
                           Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
            </DockPanel>
        </Border>

        <!-- Add Form Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="2" Panel.ZIndex="20"
                Visibility="{Binding ShowForm, Converter={StaticResource BoolToVisibilityConverter}}"
                Background="#80000000">
            <Border Background="White" CornerRadius="12" Padding="28" VerticalAlignment="Center"
                    HorizontalAlignment="Center" MinWidth="400">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Opacity="0.2" BlurRadius="40" ShadowDepth="8"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Nueva Categoria" FontSize="18" FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"/>

                    <TextBlock Text="NOMBRE" Style="{StaticResource FormLabelStyle}"/>
                    <TextBox Style="{StaticResource InputStyle}" Margin="0,0,0,12"
                             Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="ICONO (emoji)" Style="{StaticResource FormLabelStyle}"/>
                            <TextBox Style="{StaticResource InputStyle}" Text="{Binding Icon, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="TIPO" Style="{StaticResource FormLabelStyle}"/>
                            <ComboBox Style="{StaticResource ComboBoxStyle}"
                                      ItemsSource="{Binding TypeOptions}"
                                      SelectedItem="{Binding SelectedType}"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="COLOR" Style="{StaticResource FormLabelStyle}"/>
                    <ListBox ItemsSource="{Binding ColorOptions}" SelectedItem="{Binding SelectedColor}"
                             Margin="0,0,0,20" Style="{StaticResource CleanListBox}">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Width="28" Height="28" CornerRadius="6" Margin="3"
                                        Background="{Binding Converter={StaticResource HexColorToBrushConverter}}"
                                        Cursor="Hand">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                                    <Setter Property="BorderThickness" Value="2"/>
                                                    <Setter Property="BorderBrush" Value="#0F172A"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <ContentPresenter/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>

                    <TextBlock Text="{Binding FormMessage}" Foreground="#EF4444" FontSize="12" Margin="0,0,0,12"
                               Visibility="{Binding FormMessage, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Style="{StaticResource SecondaryButton}" Content="Cancelar"
                                Command="{Binding CancelCommand}" Margin="0,0,10,0"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="Crear Categoria"
                                Command="{Binding SaveCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>

        <!-- Categories Grid -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Categories}" Padding="0,0,0,24">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:Category}">
                        <Border Style="{StaticResource CardStyle}" Width="200" Margin="0,0,14,14">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="8"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="4"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <DockPanel Grid.Row="0">
                                    <Button DockPanel.Dock="Right" Style="{StaticResource DangerButton}"
                                            Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding Id}"
                                            Content="X" Padding="6,3" FontSize="10" VerticalAlignment="Top"/>
                                    <Border Width="44" Height="44" CornerRadius="12"
                                            Background="{Binding Color, Converter={StaticResource HexColorToBrushConverter}}">
                                        <TextBlock Text="{Binding Icon}" FontSize="22"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </DockPanel>
                                <TextBlock Grid.Row="2" Text="{Binding Name}" FontSize="14" FontWeight="SemiBold"
                                           Foreground="{StaticResource TextPrimaryBrush}"/>
                                <Border Grid.Row="4" CornerRadius="4" Padding="8,3" HorizontalAlignment="Left"
                                        Background="{Binding Color, Converter={StaticResource HexColorToBrushConverter}}">
                                    <TextBlock FontSize="10" FontWeight="Bold" Foreground="White">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Type}" Value="0">
                                                        <Setter Property="Text" Value="INGRESO"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="1">
                                                        <Setter Property="Text" Value="GASTO"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
